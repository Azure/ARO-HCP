apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: cert-exporter
    port: metrics
  name: cert-exporter-metrics
  namespace: '{{ .Release.Namespace }}'
spec:
  ports:
  - port: 9793
    protocol: TCP
    targetPort: 9793
    name: metrics
  selector:
    app.kubernetes.io/name: cert-exporter
