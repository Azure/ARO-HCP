apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: fleet-controller
    port: metrics
  name: fleet-controller-metrics
  namespace: '{{ .Release.Namespace }}'
spec:
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
    name: metrics
  selector:
    app.kubernetes.io/name: fleet-controller
