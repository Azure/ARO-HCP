# Freshness Rules

- Track when each domain last had corpus ingestion, calibration review, and owner confirmation.
- Treat a domain as **attention needed** when its seed history is older than one quarter or when major repo changes land in that domain without a refresh.
- Shared assets like routing, triage, and output contract should be reviewed whenever a new reviewer domain or change archetype is introduced.
