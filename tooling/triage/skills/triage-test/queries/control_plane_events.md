# Customer Control Plane Events

This query fetches the Kubernetes events pertaining to the ARO HCP cluster's control plane. These events expose what the management cluster is doing while running the Pods that make up the control plane of the ARO HCP cluster.