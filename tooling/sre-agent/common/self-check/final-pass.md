# Final Pass

Before returning:

- Output begins with `# TSG:` and contains no text before the title.
- Metadata includes `Incident envelope`.
- Required headings appear in order.
- Unknown fields are written as `Not yet determined`.
- Mitigation stays advisory-only.
