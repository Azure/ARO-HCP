# clustersService / clusterState

## Summary

Shows the Clusters Service cluster object state over time from the backend csstatedump controller.

## What to Look For

Each row is a distinct record of the object, refine the query to pick out specific fields to see how they evolve over time.

## Where to Go Next

Review `conditions/hypershift/hostedClusterConditions` to go to the next layer of the stack.
