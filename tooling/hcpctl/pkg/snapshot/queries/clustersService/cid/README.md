# clustersService / cid

## Summary

Resolves the Clusters Service internal cluster ID (cid) from the ARM resource ID.

## What to Look For

A short hash used for the cluster identifier.

## Where to Go Next

If this query returns no values, something has gone catastrophically wrong with clusters service - check `events/clustersService/events.md` to see if the deployment is healthy.
