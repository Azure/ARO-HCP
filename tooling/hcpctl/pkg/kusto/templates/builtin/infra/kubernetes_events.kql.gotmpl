{{- if .NoTruncation}}set notruncation;
{{end -}}
kubernetesEvents
| project timestamp, log, cluster
| where timestamp between({{.TimestampMin | kqlDatetime}} .. {{.TimestampMax | kqlDatetime}})
| where cluster == {{.ClusterName}}
{{- if gt .Limit 0}}
| limit {{.Limit}}
{{- end}}
| order by timestamp asc