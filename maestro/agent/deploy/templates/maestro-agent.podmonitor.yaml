apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: maestro-agent
  namespace: '{{ .Release.Namespace }}'
spec:
  selector:
    matchLabels:
      app: maestro-agent
  namespaceSelector:
    matchNames:
    - '{{ .Release.Namespace }}'
  podMetricsEndpoints:
  - path: /metrics
    port: metrics
    scheme: http
