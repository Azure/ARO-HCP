apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/name: hcp-recovery
  annotations:
    azure.workload.identity/client-id: '{{ .Values.pullBinding.workloadIdentityClientId }}'
    azure.workload.identity/tenant-id: '{{ .Values.pullBinding.workloadIdentityTenantId }}'
  name: {{ .Values.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
