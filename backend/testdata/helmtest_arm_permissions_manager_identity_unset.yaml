# This test case exercises the scenario where the ARM Permissions Manager Identity
# is not set. Note: the ARM Permissions Manager Identity is also known
# as ARM Helper identity in some other contexts.
values: ../values.yaml
name: backend-arm-permissions-manager-identity-unset
namespace: aro-hcp
testData:
  armHelperCertName: ""
  armHelperClientId: ""
  armHelperTenantId: ""
