apiVersion: v1
kind: Service
metadata:
  name: holmesgpt-svc
  namespace: '{{ .Release.Namespace }}'
spec:
  type: ClusterIP
  selector:
    app: holmesgpt
  ports:
  - name: http
    port: 80
    targetPort: 5050
    protocol: TCP
