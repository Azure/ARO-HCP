# deploy a cluster object so oadp knows it is running on azure
apiVersion: config.openshift.io/v1
kind: Infrastructure
metadata:
  name: cluster
spec:
  platformSpec:
    type: Azure
status:
  platformStatus:
    type: Azure
