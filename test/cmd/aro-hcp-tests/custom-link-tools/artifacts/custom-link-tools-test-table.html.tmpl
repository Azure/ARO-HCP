<html>
<head>
  <title>Test specific logs</title>
  <meta name="description" content="Contains links to test specific logs.">
  <link rel="stylesheet" type="text/css" href="/static/style.css">
  <link rel="stylesheet" type="text/css" href="/static/extensions/style.css">
  <link href="https://fonts.googleapis.com/css?family=Roboto:400,700" rel="stylesheet">
  <link rel="stylesheet" href="https://code.getmdl.io/1.3.0/material.indigo-pink.min.css">
  <link rel="stylesheet" type="text/css" href="/static/spyglass/spyglass.css">
  <style>
    body {
        background-color: #000;
        color: #fff;
    }
    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
    }
    th, td {
        padding: 12px 15px;
        text-align: left;
        border: 1px solid #333;
    }
    th {
        background-color: #222;
        font-weight: bold;
    }
    .status-success {
        background-color: #1b5e20;
    }
    .status-failure {
        background-color: #b71c1c;
    }
    .status-skipped {
        background-color: #f57f17;
        color: #000;
    }
    .status-pending {
        background-color: #424242;
    }
    a {
        display: inline-block;
        padding: 4px 12px;
        margin: 2px;
        border: 1px solid #4E9AF1;
        border-radius: 0.5em;
        text-decoration: none;
        color: #FFFFFF !important;
        text-align: center;
        transition: all 0.2s;
        background-color: #4E9AF1;
    }
    a:hover {
        border-color: #FFFFFF;
    }
  </style>
</head>

<body>

  <table>
    <thead>
      <tr>
        <th>Testname</th>
        <th>Status</th>
        <th>ResourceGroupName</th>
        <th>Links</th>
      </tr>
    </thead>
    <tbody>
      {{range $index, $element := .Elements}}
      <tr class="status-{{$element.Status}}">
        <td>{{$element.TestName}}</td>
        <td>{{$element.Status}}</td>
        <td>{{$element.ResourceGroupName}}</td>
        <td>
          {{range $element.Links}}
          <a target="_blank" href="{{.URL}}" title="{{.DisplayName}}">{{.DisplayName}}</a>
          {{end}}
        </td>
      </tr>
      {{end}}
    </tbody>
  </table>

</body>
</html>
