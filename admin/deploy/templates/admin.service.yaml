apiVersion: v1
kind: Service
metadata:
  name: aro-hcp-admin
  namespace: '{{ .Release.namespace  }}'
  labels:
    app: aro-hcp-admin
spec:
  type: ClusterIP
  selector:
    app: aro-hcp-admin
  ports:
    - port: 8443
      targetPort: 8443
      protocol: TCP
