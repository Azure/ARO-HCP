# Dockerfile for local development
FROM golang:1.24-alpine AS builder

WORKDIR /app

COPY observability/custom-metrics-collector/go.mod observability/custom-metrics-collector/go.sum ./
RUN go mod download

COPY observability/custom-metrics-collector/ ./

RUN go build -o custom-metrics-collector .

FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release \
    jq \
    && curl -sL https://aka.ms/InstallAzureCLIDeb | bash \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /root/

COPY --from=builder /app/custom-metrics-collector .

EXPOSE 8080

CMD ["./custom-metrics-collector"]
