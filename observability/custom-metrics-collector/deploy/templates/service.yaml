apiVersion: v1
kind: Service
metadata:
  name: custom-metrics-collector
  namespace: {{ .Release.Namespace }}
  labels:
    app: custom-metrics-collector
spec:
  selector:
    app: custom-metrics-collector
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
