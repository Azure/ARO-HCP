{{- if .Values.msiClientSecret -}}
apiVersion: v1
kind: Secret
metadata:
  name: custom-metrics-collector-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  client-secret: "{{ .Values.msiClientSecret }}"
{{- end -}}