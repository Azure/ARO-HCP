apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: '{{ .Values.host }}'
  namespace: '{{ .Release.Namespace }}'
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: '{{ .Values.volumeCapacity }}'
