apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
    helm.sh/resource-policy: keep
  name: mitigationepisodes.mgmtagent.aro-hcp.azure.com
spec:
  group: mgmtagent.aro-hcp.azure.com
  names:
    kind: MitigationEpisode
    listKind: MitigationEpisodeList
    plural: mitigationepisodes
    singular: mitigationepisode
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MitigationEpisode retains ownership and intent independently of Node lifetime.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              detector:
                type: string
              instanceID:
                type: string
              mitigator:
                type: string
              nodeName:
                type: string
              nodeUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              podUIDs:
                items:
                  description: |-
                    UID is a type that holds unique ID values, including UUIDs.  Because we
                    don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                    intent and helps make sure that UIDs and names do not get conflated.
                  type: string
                maxItems: 1024
                type: array
              policy:
                maxLength: 65536
                type: string
              poolID:
                type: string
              providerID:
                type: string
              zone:
                type: string
            required:
            - detector
            - instanceID
            - mitigator
            - nodeName
            - nodeUID
            - policy
            - poolID
            - providerID
            - zone
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentNodeUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              intent:
                description: MitigationAction is saved before making an identity-preconditioned API call.
                properties:
                  id:
                    type: string
                  kind:
                    type: string
                  lastAttemptAt:
                    format: date-time
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  pdbName:
                    type: string
                  pdbResourceVersion:
                    type: string
                  pdbUID:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                  phase:
                    type: string
                  resourceVersion:
                    type: string
                  startedAt:
                    format: date-time
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - id
                - kind
                - name
                - phase
                - resourceVersion
                - startedAt
                - uid
                type: object
              nodeDeletedAt:
                format: date-time
                type: string
              nodeDeletionAction:
                description: MitigationAction is saved before making an identity-preconditioned API call.
                properties:
                  id:
                    type: string
                  kind:
                    type: string
                  lastAttemptAt:
                    format: date-time
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  pdbName:
                    type: string
                  pdbResourceVersion:
                    type: string
                  pdbUID:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                  phase:
                    type: string
                  resourceVersion:
                    type: string
                  startedAt:
                    format: date-time
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - id
                - kind
                - name
                - phase
                - resourceVersion
                - startedAt
                - uid
                type: object
              phase:
                type: string
              recovery:
                properties:
                  deployment:
                    type: string
                  deploymentUID:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                  existingPodUIDs:
                    items:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                    maxItems: 1024
                    type: array
                  namespace:
                    type: string
                  podUID:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                  replicas:
                    format: int32
                    type: integer
                  startedAt:
                    format: date-time
                    type: string
                required:
                - deployment
                - deploymentUID
                - namespace
                - podUID
                - replicas
                - startedAt
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
