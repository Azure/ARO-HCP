$schema: "pipeline.schema.v1"
serviceGroup: Microsoft.Azure.ARO.HCP.Global
rolloutName: Global Resource Rollout
resourceGroups:
- name: {{ .global.rg }}
  subscription: {{ .global.subscription }}
  steps:
  - name: global-infra
    action: ARM
    template: Templates/Resources.bicep
    parameters: Parameters/resources.bicepparam
    deploymentLevel: ResourceGroup
  - name: svcChildZone
    action: DelegateChildZone
    deploymentLevel: ResourceGroup
    dependsOn:
      - global-infra
    parentZone:
      configRef: parentZoneName
    childZone:
      configRef: svcParentZoneName
  - name: cxChildZone
    action: DelegateChildZone
    dependsOn:
      - global-infra
    parentZone:
      configRef: parentZoneName
    childZone:
      configRef: baseDnsZoneName