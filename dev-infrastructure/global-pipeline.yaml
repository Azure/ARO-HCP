$schema: "pipeline.schema.v1"
serviceGroup: Microsoft.Azure.ARO.HCP.Global
rolloutName: Global Resource Rollout
resourceGroups:
- name: {{ .global.rg }}
  subscription: {{ .global.subscription }}
  steps:
  - name: global-infra
    action: ARM
    template: Templates/Resources.bicep
    parameters: Parameters/resources.bicepparam
    deploymentLevel: ResourceGroup
  - name: svcChildZone
    action: DelegateChildZone
    parentZone:
      configRef: {{ .global.parentZoneName }}
    childZone:
      configRef: {{ .global.svcParentZoneName }}
    dependsOn:
      - global-infra
  - name: cxChildZone
    action: DelegateChildZone
    parentZone:
      configRef: {{ .global.parentZoneName }}
    childZone:
      configRef: {{ .global.baseDnsZoneName }}
    dependsOn:
      - global-infra