apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cihealth.serviceAccountName" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "cihealth.labels" . | nindent 4 }}
  annotations:
    azure.workload.identity/client-id: "{{ .Values.msiClientId }}"
