---
# Source: secrets-store-sync-controller/crds/secret-sync.x-k8s.io_secretsyncs.yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: secretsyncs.secret-sync.x-k8s.io
spec:
  group: secret-sync.x-k8s.io
  names:
    kind: SecretSync
    listKind: SecretSyncList
    plural: secretsyncs
    singular: secretsync
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretSync represents the desired state and observed state of the secret synchronization process. The SecretSync name is used to as the secret object created by the controller.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SecretSyncSpec defines the desired state for synchronizing secret.
            properties:
              forceSynchronization:
                description: forceSynchronization can be used to force the secret synchronization. The secret synchronization is triggered, by changing the value in this field. This field is not used to resolve synchronization conflicts. It is not related with the force query parameter in the Apply operation. https://kubernetes.io/docs/reference/using-api/server-side-apply/#conflicts
                maxLength: 253
                pattern: ^[A-Za-z0-9]([-A-Za-z0-9]+([-._a-zA-Z0-9]?[A-Za-z0-9])*)?
                type: string
              secretObject:
                description: secretObject specifies the configuration for the synchronized Kubernetes secret object.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: 'annotations contains key-value pairs representing annotations associated with the Kubernetes secret object. The following annotation prefix is reserved: secrets-store.sync.x-k8s.io/. Creation fails if the annotation key is specified in the SecretSync object by the user.'
                    type: object
                    x-kubernetes-validations:
                    - message: Annotations should have < 253 characters for both keys and values.
                      rule: (self.all(x, x.size() < 253 && self[x].size() < 253) == true)
                    - message: Annotations should not contain secrets-store.sync.x-k8s.io. This key is reserved for the controller.
                      rule: (self.all(x, x.startsWith('secrets-store.sync.x-k8s.io') == false))
                  data:
                    description: data is a slice of SecretObjectData containing secret data source from the Secret Provider Class and the corresponding data field key used in the Kubernetes secret object.
                    items:
                      description: SecretObjectData defines the desired state of synchronized data within a Kubernetes secret object.
                      properties:
                        sourcePath:
                          description: sourcePath is the data source value of the secret defined in the Secret Provider Class. This matches the path of a file in the MountResponse returned from the provider.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[A-Za-z0-9.]([-A-Za-z0-9]+([-._a-zA-Z0-9]?[A-Za-z0-9])*)?(\/([0-9]+))*$
                          type: string
                        targetKey:
                          description: 'targetKey is the key in the Kubernetes secret''s data field as described in the Kubernetes API reference: https://kubernetes.io/docs/reference/kubernetes-api/config-and-storage-resources/secret-v1/'
                          maxLength: 253
                          minLength: 1
                          pattern: ^[A-Za-z0-9.]([-A-Za-z0-9]+([-._a-zA-Z0-9]?[A-Za-z0-9])*)?(\/([0-9]+))*$
                          type: string
                      required:
                      - sourcePath
                      - targetKey
                      type: object
                    minItems: 1
                    type: array
                    x-kubernetes-list-map-keys:
                    - targetKey
                    x-kubernetes-list-type: map
                  labels:
                    additionalProperties:
                      type: string
                    description: 'labels contains key-value pairs representing labels associated with the Kubernetes secret object. The labels are used to identify the secret object created by the controller. On secret creation, the following label is added: secrets-store.sync.x-k8s.io/secretsync=<secret-sync-name>. The following label prefix is reserved: secrets-store.sync.x-k8s.io/. Creation fails if the label is specified in the SecretSync object with a different value. On secret update, if the validation admission policy is set, the controller will check if the label secrets-store.sync.x-k8s.io/secretsync=<secret-sync-name> is present. If the label is not present, controller fails to update the secret.'
                    type: object
                    x-kubernetes-validations:
                    - message: Labels should have < 63 characters for both keys and values.
                      rule: (self.all(x, x.size() < 63 && self[x].size() < 63) == true)
                    - message: Labels should not contain secrets-store.sync.x-k8s.io. This key is reserved for the controller.
                      rule: (self.all(x, x.startsWith('secrets-store.sync.x-k8s.io') == false))
                  type:
                    description: type specifies the type of the Kubernetes secret object, e.g. "Opaque";"kubernetes.io/basic-auth";"kubernetes.io/ssh-auth";"kubernetes.io/tls" The controller must have permission to create secrets of the specified type.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - data
                - type
                type: object
              secretProviderClassName:
                description: secretProviderClassName specifies the name of the secret provider class used to pass information to access the secret store.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              secretSyncControllerName:
                default: ""
                description: secretSyncControllerName specifies the name of the secret sync controller used to synchronize the secret.
                type: string
              serviceAccountName:
                description: serviceAccountName specifies the name of the service account used to access the secret store. The audience field in the service account token must be passed as parameter in the controller configuration. The audience is used when requesting a token from the API server for the service account; the supported audiences are defined by each provider.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
            required:
            - secretObject
            - secretProviderClassName
            - serviceAccountName
            type: object
          status:
            description: SecretSyncStatus defines the observed state of the secret synchronization process.
            properties:
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current state of this API Resource. --- This struct is intended for direct use as an array at the field path .status.conditions.  For example, \n type FooStatus struct{ // Represents the observations of a foo's current state. // Known .status.conditions.type are: \"Available\", \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge // +listType=map // +listMapKey=type Conditions []metav1.Condition `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase. --- Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSuccessfulSyncTime:
                description: lastSuccessfulSyncTime represents the last time the secret was retrieved from the Provider and updated.
                format: date-time
                type: string
              syncHash:
                description: 'syncHash contains the hash of the secret object data, data from the SecretProviderClass (e.g. UID, and metadata.generation), and similar data from the SecretSync. This hash is used to determine if the secret changed. The hash is calculated using the HMAC (Hash-based Message Authentication Code) algorithm, using bcrypt hashing, with the SecretsSync''s UID as the key. The secret is updated if: 1. the hash is different 2. the lastSuccessfulSyncTime indicates a rotation is required - the rotation poll interval is passed as a parameter in the controller configuration 3. the SecretUpdateStatus is ''Failed'''
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
# Source: secrets-store-sync-controller/templates/secrets-store-sync-controller.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/instance: controller-manager
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/name: serviceaccount
    app.kubernetes.io/part-of: secrets-store-sync-controller
    secrets-store.io/system: "true"
  name: "secrets-store-sync-controller-manager"
  namespace: 'secret-sync-controller'
  annotations:
---
# Source: secrets-store-sync-controller/templates/secret-sync-admin.clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: secret-sync-admin
  labels:
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
rules:
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs/status
  verbs:
  - get
---
# Source: secrets-store-sync-controller/templates/secrets-store-sync-controller.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: secrets-store-sync-controller-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - patch
- apiGroups:
  - ""
  resources:
  - serviceaccounts/token
  verbs:
  - create
- apiGroups:
  - secrets-store.csi.x-k8s.io
  resources:
  - secretproviderclasses
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs/status
  verbs:
  - get
  - patch
  - update
---
# Source: secrets-store-sync-controller/templates/secretsync_editor_role.yaml
# permissions for end users to edit secretsyncs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: secretsync-editor-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/part-of: secrets-store-sync-controller
    app.kubernetes.io/managed-by: Helm
  name: secretsync-editor-role
rules:
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs/status
  verbs:
  - get
---
# Source: secrets-store-sync-controller/templates/secretsync_viewer_role.yaml
# permissions for end users to view secretsyncs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: secretsync-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/part-of: secrets-store-sync-controller
    app.kubernetes.io/managed-by: Helm
  name: secretsync-viewer-role
rules:
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secret-sync.x-k8s.io
  resources:
  - secretsyncs/status
  verbs:
  - get
---
# Source: secrets-store-sync-controller/templates/secrets-store-sync-controller.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/instance: manager-rolebinding
    
    app.kubernetes.io/name: clusterrolebinding
    app.kubernetes.io/part-of: secrets-store-sync-controller
    secrets-store.io/system: "true"
  name: secrets-store-sync-controller-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: secrets-store-sync-controller-manager-role
subjects:
  - kind: ServiceAccount
    name: "secrets-store-sync-controller-manager"
    namespace: secret-sync-controller
---
# Source: secrets-store-sync-controller/templates/leader_election_role.yaml
# permissions to do leader election.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    app.kubernetes.io/name: role
    app.kubernetes.io/instance: leader-election-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/part-of: secrets-store-sync-controller
    app.kubernetes.io/managed-by: Helm
    secrets-store.io/system: "true"
  name: secrets-store-sync-controller-leader-election-role
  namespace: 'secret-sync-controller'
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
# Source: secrets-store-sync-controller/templates/leader_election_role_binding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/name: rolebinding
    app.kubernetes.io/instance: leader-election-rolebinding
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: secrets-store-sync-controller
    app.kubernetes.io/part-of: secrets-store-sync-controller
    app.kubernetes.io/managed-by: Helm
    secrets-store.io/system: "true"
  name: secrets-store-sync-controller-leader-election-rolebinding
  namespace: 'secret-sync-controller'
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: secrets-store-sync-controller-leader-election-role
subjects:
  - kind: ServiceAccount
    name: "secrets-store-sync-controller-manager"
    namespace: secret-sync-controller
---
# Source: secrets-store-sync-controller/templates/secrets-store-sync-controller.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: secrets-store-sync-controller-manager
  namespace: 'secret-sync-controller'
  labels:
    app.kubernetes.io/component: manager
    app.kubernetes.io/created-by: secrets-store-sync-controller
    control-plane: controller-manager
    app.kubernetes.io/name: deployment
    app.kubernetes.io/part-of: secrets-store-sync-controller
    app.kubernetes.io/instance: controller-manager
    secrets-store.io/system: "true"
spec:
  selector:
    matchLabels:
      control-plane: controller-manager
      secrets-store.io/system: "true"
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: controller-manager
        secrets-store.io/system: "true"
        app: secrets-store-sync-controller
    spec:
      nodeSelector:
        kubernetes.io/os: linux
      tolerations:
      - operator: Exists
      containers:
      - args:
        - --endpoint=unix:///provider/azure.sock
        - --construct-pem-chain=true
        - --healthz-port=8989
        - --healthz-path=/healthz
        - --healthz-timeout=5s
        image: mcr.microsoft.com/oss/v2/azure/secrets-store/provider-azure@sha256:1234567890
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: 8989
          initialDelaySeconds: 5
          periodSeconds: 30
          timeoutSeconds: 10
        name: provider-azure-installer
        ports:
        - containerPort: 8898
          name: metrics
          protocol: TCP
        resources:
          limits:
            cpu: 50m
            memory: 100Mi
          requests:
            cpu: 50m
            memory: 100Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          runAsUser: 0
        volumeMounts:
        - mountPath: /provider
          name: providervol
      - name: manager
        image: arohcpsvcdev.azurecr.io/secrets-store-sync/controller@sha256:1234567890
        imagePullPolicy: 'IfNotPresent'
        args:
        - --provider-volume=/provider
        - --token-request-audience=
        - --health-probe-bind-address=:8081
        - --metrics-bind-address=:8085
        - --leader-elect
        env:
          - name: SYNC_CONTROLLER_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: SYNC_CONTROLLER_POD_UID
            valueFrom:
              fieldRef:
                fieldPath: metadata.uid
          - name: SYNC_CONTROLLER_POD_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        ports:
        - name: metrics
          containerPort: 8085
          protocol: TCP
        securityContext:
          null
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        command:
        - /secrets-store-sync-controller
        volumeMounts:
        - mountPath: "/provider"
          name: providervol
      serviceAccountName: "secrets-store-sync-controller-manager"
      terminationGracePeriodSeconds: 10
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: aro-hcp.azure.com/role
                operator: In
                values:
                - infra
      tolerations:
      - effect: NoSchedule
        key: infra
        operator: "Equal"
        value: "true"
      volumes:
      - name: providervol
        hostPath:
          path: "/var/run/secrets-store-sync-providers"
          type: DirectoryOrCreate
---
# Source: secrets-store-sync-controller/templates/create_update_secrets_types.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-create-update-policy"
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["secrets"]
        scope: '*'
  variables:
    - name: hasOneSecretSyncOwner
      expression: "(size(object.metadata.ownerReferences) == 1 && object.metadata.ownerReferences.all(o, o.kind == 'SecretSync' && o.apiVersion.startsWith('secret-sync.x-k8s.io/') && o.name == object.metadata.name))"
    - name: allowedSecretTypes
      expression: "(object.type in [\"Opaque\", \"kubernetes.io/basic-auth\", \"bootstrap.kubernetes.io/token\", \"kubernetes.io/dockerconfigjson\", \"kubernetes.io/dockercfg\", \"kubernetes.io/ssh-auth\", \"kubernetes.io/tls\"])"
  validations:
    - expression: "variables.allowedSecretTypes == true && variables.hasOneSecretSyncOwner == true"
      message: "Only secrets with types defined in the allowedSecretTypes are allowed."
      messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. The controller can only create or update secrets in the allowed types list with a single secretsync owner.'"
---
# Source: secrets-store-sync-controller/templates/create_update_token_request_deny.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-create-update-token-deny-policy"
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["secrets"]
        scope: '*'
  variables:
    - name: deniedSecretTypes
      expression: "(object.type in [\"kubernetes.io/service-account-token\"])"
  validations:
    - expression: "variables.deniedSecretTypes == false"
      message: "Only secrets with types defined in the allowedSecretTypes are allowed."
      messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. The controller is not allowed to create or update secrets with this type.'"
---
# Source: secrets-store-sync-controller/templates/delete_secrets.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-delete-policy"
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   [""]
      apiVersions: ["v1"]
      operations:  ["DELETE"]
      resources:   ["secrets"]
      scope: '*'
  validations:
  - expression: "request.operation == 'delete'"
    message: "The controller is not allowed to delete secrets."
    messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secrets in the ' + string(request.namespace) + ' namespace. The controller is not allowed to delete secrets.'"
---
# Source: secrets-store-sync-controller/templates/update_owners_check_old_object.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-update-check-oldobject-policy"
spec:
  failurePolicy: Fail
  paramKind:
    apiVersion: v1
    kind: ConfigMap 
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   [""]
      apiVersions: ["v1"]
      operations:  ["UPDATE"]
      resources:   ["secrets"]
      scope: '*'
  variables:
  - name: hasOneSecretSyncOwner
    expression: "has(oldObject.metadata.ownerReferences) && (size(oldObject.metadata.ownerReferences) == 1 && oldObject.metadata.ownerReferences.all(o, o.kind == 'SecretSync' && o.apiVersion.startsWith('secret-sync.x-k8s.io/') && o.name == object.metadata.name))"
  - name: allowedSecretTypes
    expression: "(object.type in [\"Opaque\", \"kubernetes.io/basic-auth\", \"bootstrap.kubernetes.io/token\", \"kubernetes.io/dockerconfigjson\", \"kubernetes.io/dockercfg\", \"kubernetes.io/ssh-auth\", \"kubernetes.io/tls\"])"
  validations:
  - expression: "variables.allowedSecretTypes == true && variables.hasOneSecretSyncOwner == true"
    message: "Only secrets with one secret sync owner and with types defined in the allowedSecretTypes list can be updated by the controller"
    messageExpression: "string(params.data.controllerName)  + ' has failed to ' +  string(request.operation) + ' old secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. The controller can only update secrets in the allowed types list with a single secrets-store-sync-controller owner.'"
    reason: "Forbidden"
---
# Source: secrets-store-sync-controller/templates/update_secrets_based_on_label.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-update-label-policy"
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   [""]
      apiVersions: ["v1"]
      operations:  ["UPDATE"]
      resources:   ["secrets"]
      scope: '*'
  variables:
  - name: oldSecretHasLabels
    expression: "has(oldObject.metadata.labels) ? true : false"
  - name: oldSecretHasExpectedLabelKey
    expression: "variables.oldSecretHasLabels && ('secrets-store.sync.x-k8s.io' in oldObject.metadata.labels) ? true : false"
  - name: oldSecretHasExpectedLabelValue
    expression: "'' == oldObject.metadata.labels['secrets-store.sync.x-k8s.io'] ? true : false"
  validations:
  - expression: "variables.oldSecretHasExpectedLabelKey && variables.oldSecretHasExpectedLabelValue"
    message: "Only secrets with the correct label can be updated"
    messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace because it does not have the correct label. Delete the secret and force the controller to recreate it with the correct label.'"
---
# Source: secrets-store-sync-controller/templates/validate_annotation_format_secret_sync.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-validate-annotation-policy"
spec:
  failurePolicy: Fail
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   ["secret-sync.x-k8s.io"]
      apiVersions: ["*"]
      operations:  ["CREATE", "UPDATE"]
      resources:   ["secretsyncs"]
      scope: '*'
  variables:
  - name: secretHasAnnotation
    expression: "has(object.spec.secretObject.annotations) ? true : false"
  - name: secretHasCorrectAnnotationsFormat
    expression: "variables.secretHasAnnotation && object.spec.secretObject.annotations.all(x, size(x) < 253 && x.matches('^([A-Za-z0-9][-A-Za-z0-9_.]*[A-Za-z0-9])?(/[A-Za-z0-9]([-A-Za-z0-9_.]*[A-Za-z0-9])*)?$') == true)"
  - name: secretHasCorrectAnnotationsValueFormat
    expression: "variables.secretHasAnnotation && object.spec.secretObject.annotations.all(x, size(object.spec.secretObject.annotations[x]) < 63 && object.spec.secretObject.annotations[x].matches('^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$') == true)"
  validations:
  - expression: "variables.secretHasAnnotation == false || (variables.secretHasCorrectAnnotationsFormat && variables.secretHasCorrectAnnotationsValueFormat) == true"
    message: "One of the annotations applied on the secret has an invalid format. Update the annotation and try again."
    messageExpression: "string(request.userInfo.username)  + ' has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace. One of the annotations applied on the secret has an invalid format. Update the annotation and try again.'"
    reason: "Invalid"
---
# Source: secrets-store-sync-controller/templates/validate_label_format_secret_sync.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-validate-label-policy"
spec:
  failurePolicy: Fail
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   ["secret-sync.x-k8s.io"]
      apiVersions: ["*"]
      operations:  ["CREATE", "UPDATE"]
      resources:   ["secretsyncs"]
      scope: '*'
  variables:
  - name: secretHasLabel
    expression: "has(object.spec.secretObject.labels) ? true : false"
  - name: secretHasCorrectLabelsFormat
    expression: "variables.secretHasLabel && object.spec.secretObject.labels.all(x, size(x) < 253 && x.matches('^([A-Za-z0-9][-A-Za-z0-9_.]*[A-Za-z0-9])?(/[A-Za-z0-9]([-A-Za-z0-9_.]*[A-Za-z0-9])*)?$') == true)"
  - name: secretHasCorrectLabelsValueFormat
    expression: "variables.secretHasLabel && object.spec.secretObject.labels.all(x, size(object.spec.secretObject.labels[x]) < 63 && object.spec.secretObject.labels[x].matches('^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$') == true)"
  validations:
  - expression: "variables.secretHasLabel == false || (variables.secretHasCorrectLabelsFormat && variables.secretHasCorrectLabelsValueFormat) == true"
    message: "One of the labels applied on the secret has an invalid format. Update the label and try again."
    messageExpression: "string(request.userInfo.username)  + ' has failed to ' +  string(request.operation) + ' secret with ' + string(object.type) + ' type ' + 'in the ' + string(request.namespace) + ' namespace because it does not have the correct label. Delete the secret and force the controller to recreate it with the correct label.'"
    reason: "Invalid"
---
# Source: secrets-store-sync-controller/templates/validate_token_config.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicy
metadata:
  name: "secrets-store-sync-controller-validate-token-policy"
spec:
  failurePolicy: Fail
  matchConditions:
    - name: 'user-is-secrets-store-sync-controller'
      expression: "request.userInfo.username == 'system:serviceaccount:secret-sync-controller:secrets-store-sync-controller-manager'"
  matchConstraints:
    matchPolicy: Equivalent
    namespaceSelector: { }
    objectSelector: { }
    resourceRules:
    - apiGroups:   [""]
      apiVersions: ["v1"]
      operations:  ["CREATE"]
      resources:   ["serviceaccounts/token"]
      scope: '*'
  variables:
  - name: expirationSeconds
    expression: "string(object.spec.expirationSeconds) == '600'"
  - name: hasCorrectAudience
    expression: "object.spec.audiences.exists(w, w == '%!s(<nil>)')"
  - name: requestHasOnlyOneAudience
    expression: "object.spec.audiences.size() == 1"
  validations:
  - expression: "variables.hasCorrectAudience == true && variables.expirationSeconds == true && variables.requestHasOnlyOneAudience == true"
    message: "'Creating a serviceaccount token has failed because the configuration isn't correct.'"
    messageExpression: "'secrets-store-sync-controller has failed to ' +  string(request.operation) + ' ' + string(request.name) + ' token in the ' + string(request.namespace) + ' namespace. Check the configuration.'"
    reason: "Forbidden"
---
# Source: secrets-store-sync-controller/templates/VAPB_create_update_secrets_type.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-create-update-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-create-update-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_create_update_token_request_deny.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-create-update-token-deny-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-create-update-token-deny-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_delete_secrets.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-delete-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-delete-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_update_owners_check_old_object.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-update-owners-check-oldobject-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-update-owners-check-oldobject-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_update_secrets_based_on_label.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-update-label-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-update-label-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_validate_annotation_format_secret_sync.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-validate-annotation-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-validate-annotation-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_validate_label_format_secret_sync.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-validate-label-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-validate-label-policy"
  validationActions: [Deny]
---
# Source: secrets-store-sync-controller/templates/VAPB_validate_token_config.yaml
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "secrets-store-sync-controller-validate-token-policy-binding"
spec:
  policyName: "secrets-store-sync-controller-validate-token-policy"
  validationActions: [Deny]

